[SlaveServer]
# 内网 master 主动从 VPS/DMZ slave 拉取任务。
# 部署时可用 SLAVE_HOST / SLAVE_PORT 覆盖。
host="8.222.226.16"
port="14548"

[MasterServer]
# Jenkins 留在内网。凭据优先通过环境变量注入：
#   SGLANG_JENKINS_USER=...
#   SGLANG_JENKINS_TOKEN=...
jenkins_path="jenkins.svc.cambricon.com/dist/job/SGLANG/job/DEBUG/job/sglang_ci/"
jenkins_user="chenxiaobing"
jenkins_token="113e44d88d19688907daa0a42b36e74aa7"

# 参数名必须与 Jenkins job 定义一致。
jenkins_params="repo;timestamp;pr_id;task_id;trigger_type;repo_url;git_ref;commit_sha"
